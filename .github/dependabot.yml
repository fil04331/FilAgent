# ===========================================================================
# FilAgent - Configuration Dependabot
# ===========================================================================
# Automatise les mises à jour de sécurité et de dépendances
# Les PRs sont créées automatiquement pour les vulnérabilités et mises à jour
# ===========================================================================

version: 2
updates:
  # Python dependencies via pip/PDM
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "monthly"  # Changed from weekly to reduce PR volume
      day: "monday"
      time: "08:00"
      timezone: "America/Montreal"
    open-pull-requests-limit: 3  # Reduced from 5
    # Group all non-security updates together
    groups:
      python-dependencies:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    # Labels pour les PRs
    labels:
      - "dependencies"
      - "python"
      - "automated"
      - "security"
    # Stratégie de commit
    commit-message:
      prefix: "chore"
      prefix-development: "chore"
      include: "scope"
    # Reviewers automatiques
    reviewers:
      - "fil04331"
    # Ignorer certaines mises à jour
    ignore:
      # Ne pas mettre à jour automatiquement les versions majeures
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
      # Exceptions pour packages critiques (mise à jour manuelle uniquement)
      - dependency-name: "cryptography"
        update-types: ["version-update:semver-major", "version-update:semver-minor"]
      - dependency-name: "pydantic"
        update-types: ["version-update:semver-major"]

  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"  # Changed from weekly
      day: "monday"
      time: "08:00"
      timezone: "America/Montreal"
    open-pull-requests-limit: 2  # Reduced from 3
    # Group all GitHub Actions updates together
    groups:
      github-actions:
        patterns:
          - "*"
    labels:
      - "dependencies"
      - "github-actions"
      - "ci/cd"
      - "automated"
    commit-message:
      prefix: "ci"
      include: "scope"
    reviewers:
      - "fil04331"
