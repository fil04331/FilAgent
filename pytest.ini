[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Options
addopts =
    -v
    --strict-markers
    --tb=short
    --color=yes
    --disable-warnings

# Marqueurs
markers =
    slow: tests lents
    integration: tests d'intégration
    unit: tests unitaires
    memory: tests de mémoire
    tools: tests d'outils
    compliance: tests de conformité (Loi 25, RGPD)
    e2e: tests end-to-end
    htn: tests HTN (Hierarchical Task Network) planning
    resilience: tests de résilience et fallbacks
    fixtures: tests validating fixture functionality
    performance: tests de performance et de charge
    requires_llama_cpp: tests requiring llama-cpp-python (optional ml dependency)
    coverage: tests for improving code coverage
