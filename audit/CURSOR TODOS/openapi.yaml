openapi: 3.0.3
info:
  title: FilAgent API
  version: 0.1.0
  description: |
    **FilAgent** - Agent LLM avec Gouvernance, Traçabilité et Conformité Légale Complète
    
    ## 🎯 Architecture de Conformité
    
    FilAgent implémente une stack de conformité multi-niveaux pour respecter:
    - **Loi 25 (Québec)** - Transparence des décisions automatisées (ADM)
    - **RGPD (UE)** - Protection des données personnelles
    - **AI Act (UE)** - Exigences de traçabilité et transparence
    - **NIST AI RMF 1.0** - Cadre de gestion des risques de l'IA
    
    ## 🔒 Middlewares de Conformité
    
    Chaque requête traverse automatiquement:
    1. **EventLogger** - Journalisation JSONL compatible OpenTelemetry
    2. **WormLogger** - Logs append-only avec arbre de Merkle pour intégrité
    3. **DRManager** - Génération de Decision Records signés EdDSA
    4. **ProvenanceTracker** - Métadonnées W3C PROV-JSON pour traçabilité complète
    5. **PIIRedactor** - Masquage automatique des données sensibles
    6. **ConstraintsEngine** - Validation des sorties selon guardrails configurés
    
    ## 🛠️ Capacités Agentiques
    
    - **Raisonnement multi-étapes** (max 10 iterations)
    - **Appels d'outils automatiques** (Python sandbox, file reader, calculator)
    - **Mémoire conversationnelle** persistante (SQLite)
    - **Mémoire sémantique** avec recherche vectorielle (FAISS)
    
    ## 📊 Observabilité
    
    - Logs structurés JSONL avec trace_id/span_id
    - Decision Records pour chaque action significative
    - Provenance complète des artefacts générés
    - Métriques d'usage (tokens, latence, outils utilisés)
    
  contact:
    name: FilAgent Support
    url: https://github.com/fil04331/FilAgent
  license:
    name: Proprietary
    url: https://github.com/fil04331/FilAgent

servers:
  - url: http://localhost:8000
    description: Development server
  - url: https://api.filagent.example.com
    description: Production server (example)

tags:
  - name: chat
    description: Endpoints de conversation avec l'agent
  - name: health
    description: Endpoints de santé et monitoring
  - name: memory
    description: Endpoints de gestion de la mémoire
  - name: compliance
    description: Endpoints de conformité et audit

paths:
  /:
    get:
      summary: Health check basique
      description: |
        Endpoint léger pour vérifier que le service répond.
        Utilisé par les load balancers et monitoring basique.
      operationId: root_health_check
      tags:
        - health
      responses:
        '200':
          description: Service opérationnel
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BasicHealthResponse'
              example:
                service: FilAgent
                version: "0.1.0"
                status: running

  /health:
    get:
      summary: Health check détaillé
      description: |
        Vérification approfondie de tous les composants critiques:
        - **Model**: Modèle LLM chargé et fonctionnel
        - **Database**: Connexion SQLite pour mémoire épisodique
        - **Logging**: Système de journalisation JSONL + WORM
        
        **Status possible:**
        - `healthy`: Tous les composants opérationnels
        - `degraded`: Au moins un composant défaillant
        
        **Note de conformité**: Ce endpoint ne crée JAMAIS de ressources (read-only).
        Les checks de logging vérifient l'existence des directories sans les créer.
      operationId: detailed_health_check
      tags:
        - health
      responses:
        '200':
          description: Health check réussi (peut être degraded)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailedHealthResponse'
              examples:
                healthy:
                  summary: Tous les composants opérationnels
                  value:
                    status: healthy
                    timestamp: "2025-10-27T21:39:00.000Z"
                    components:
                      model: true
                      database: true
                      logging: true
                degraded:
                  summary: Un composant défaillant
                  value:
                    status: degraded
                    timestamp: "2025-10-27T21:39:00.000Z"
                    components:
                      model: false
                      database: true
                      logging: true

  /chat:
    post:
      summary: Conversation avec l'agent
      description: |
        Endpoint principal pour interagir avec l'agent LLM.
        
        ## 🔄 Flux de Traitement
        
        1. **Validation** des messages et paramètres
        2. **Récupération** du contexte conversationnel (mémoire épisodique)
        3. **Génération** via modèle LLM avec parsing tool calls
        4. **Exécution** automatique des outils si détectés
        5. **Logging** complet (events, DR, provenance)
        6. **Retour** de la réponse avec métadonnées
        
        ## 🛡️ Conformité Automatique
        
        Pour chaque requête:
        - ✅ **Event log** `conversation.start` avec trace_id
        - ✅ **Event logs** `tool.call` pour chaque outil utilisé
        - ✅ **Decision Record** si outils utilisés ou actions significatives
        - ✅ **Provenance PROV-JSON** pour traçabilité complète
        - ✅ **Event log** `conversation.end` avec métriques
        
        ## 🔒 Sécurité
        
        - **PII Redaction**: Masquage automatique avant logging
        - **Guardrails**: Validation des sorties selon `policies.yaml`
        - **Sandboxing**: Exécution isolée des outils (quotas CPU/mémoire)
        - **RBAC**: Permissions vérifiées selon rôle utilisateur
        
        ## 📊 Métriques Retournées
        
        - **tokens**: prompt_tokens, completion_tokens, total_tokens
        - **tools_used**: Liste des outils exécutés
        - **iterations**: Nombre de tours de raisonnement
        - **conversation_id**: ID pour récupération historique
      operationId: chat_with_agent
      tags:
        - chat
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatRequest'
            examples:
              simple_question:
                summary: Question simple sans outils
                value:
                  messages:
                    - role: user
                      content: "Explique-moi comment fonctionne un arbre de Merkle"
                  conversation_id: "conv-12345"
              
              tool_usage:
                summary: Requête nécessitant un outil (calcul)
                value:
                  messages:
                    - role: user
                      content: "Calcule la factorielle de 10"
                  conversation_id: "conv-67890"
                  task_id: "task-calc-001"
              
              multi_turn:
                summary: Conversation multi-tours avec contexte
                value:
                  messages:
                    - role: user
                      content: "Écris un script Python pour lire un fichier CSV"
                    - role: assistant
                      content: "Voici un script Python pour lire un fichier CSV..."
                    - role: user
                      content: "Maintenant ajoute le traitement des erreurs"
                  conversation_id: "conv-11111"
              
              with_custom_params:
                summary: Avec paramètres personnalisés
                value:
                  messages:
                    - role: user
                      content: "Génère du code créatif"
                  conversation_id: "conv-22222"
                  temperature: 0.8
                  max_tokens: 1000
      
      responses:
        '200':
          description: Réponse de l'agent générée avec succès
          headers:
            X-Trace-ID:
              description: Trace ID pour debugging (correspond au trace_id dans les logs)
              schema:
                type: string
                example: "9b1d1f2a3c4d5e6f"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatResponse'
              examples:
                simple_response:
                  summary: Réponse textuelle simple
                  value:
                    id: "chatcmpl-1698438000"
                    object: "chat.completion"
                    created: 1698438000
                    model: "llama-3"
                    choices:
                      - index: 0
                        message:
                          role: assistant
                          content: "Un arbre de Merkle est une structure de données cryptographique utilisée pour vérifier l'intégrité de grands ensembles de données..."
                        finish_reason: stop
                    usage:
                      prompt_tokens: 25
                      completion_tokens: 150
                      total_tokens: 175
                
                with_tools:
                  summary: Réponse avec utilisation d'outils
                  value:
                    id: "chatcmpl-1698438100"
                    object: "chat.completion"
                    created: 1698438100
                    model: "llama-3"
                    choices:
                      - index: 0
                        message:
                          role: assistant
                          content: "J'ai calculé la factorielle de 10 en utilisant l'outil Python sandbox. Le résultat est 3,628,800."
                        finish_reason: stop
                        tools_used:
                          - python_sandbox
                    usage:
                      prompt_tokens: 30
                      completion_tokens: 80
                      total_tokens: 110
        
        '400':
          description: Requête invalide
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                no_user_message:
                  summary: Aucun message utilisateur fourni
                  value:
                    detail: "No user message provided"
                invalid_format:
                  summary: Format de message invalide
                  value:
                    detail: "Invalid message format: 'role' and 'content' required"
        
        '500':
          description: Erreur serveur interne
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                agent_error:
                  summary: Erreur lors de la génération
                  value:
                    detail: "Agent error: Generation timeout after 60s"
                model_error:
                  summary: Modèle non disponible
                  value:
                    detail: "Failed to initialize agent: Model not loaded"

  /conversations/{conversation_id}:
    get:
      summary: Récupérer l'historique d'une conversation
      description: |
        Retourne tous les messages d'une conversation persistés en mémoire épisodique.
        
        ## 📦 Contenu Retourné
        
        - **messages**: Liste complète des échanges (user + assistant)
        - **metadata**: Informations par message (timestamp, task_id, type)
        - **TTL**: Les conversations sont conservées selon `retention.yaml` (défaut: 30 jours)
        
        ## 🔒 Conformité
        
        - **PII**: Les données sensibles sont redactées avant stockage
        - **RBAC**: Vérification que l'utilisateur a accès à cette conversation
        - **Audit**: Accès logué dans les événements
        
        ## 💡 Cas d'usage
        
        - **Reprise de conversation**: Recharger le contexte pour continuer
        - **Audit**: Inspection des échanges pour conformité
        - **Export**: Extraction des données pour portabilité (RGPD)
      operationId: get_conversation_history
      tags:
        - memory
      parameters:
        - name: conversation_id
          in: path
          required: true
          description: Identifiant unique de la conversation
          schema:
            type: string
            pattern: '^[a-zA-Z0-9\-_]+$'
          example: "conv-12345"
      
      responses:
        '200':
          description: Historique de conversation récupéré
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConversationHistory'
              examples:
                simple_conversation:
                  summary: Conversation simple avec 2 tours
                  value:
                    conversation_id: "conv-12345"
                    messages:
                      - role: user
                        content: "Bonjour, peux-tu m'aider ?"
                        timestamp: "2025-10-27T21:30:00.000Z"
                        message_type: text
                        metadata: {}
                      - role: assistant
                        content: "Bonjour ! Bien sûr, comment puis-je vous aider ?"
                        timestamp: "2025-10-27T21:30:02.000Z"
                        message_type: text
                        metadata:
                          tokens_used: 25
                
                with_task_ids:
                  summary: Conversation avec task_id tracking
                  value:
                    conversation_id: "conv-67890"
                    messages:
                      - role: user
                        content: "Analyse ce document"
                        task_id: "task-001"
                        timestamp: "2025-10-27T21:35:00.000Z"
                        message_type: text
                      - role: assistant
                        content: "J'ai analysé le document. Voici les points clés..."
                        task_id: "task-001"
                        timestamp: "2025-10-27T21:35:15.000Z"
                        message_type: text
                        metadata:
                          tools_used: ["file_read"]
        
        '404':
          description: Conversation non trouvée
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                detail: "Conversation not found"

components:
  schemas:
    # ==================== Request/Response Schemas ====================
    
    Message:
      type: object
      required:
        - role
        - content
      properties:
        role:
          type: string
          enum: [user, assistant, system]
          description: |
            Rôle de l'émetteur du message:
            - `user`: Message de l'utilisateur
            - `assistant`: Réponse de l'agent
            - `system`: Instructions système (rarement utilisé)
          example: user
        content:
          type: string
          description: Contenu textuel du message
          minLength: 1
          maxLength: 50000
          example: "Explique-moi la théorie de la relativité"
      example:
        role: user
        content: "Quelle est la différence entre un arbre B et un arbre B+ ?"
    
    ChatRequest:
      type: object
      required:
        - messages
      properties:
        messages:
          type: array
          description: |
            Historique de la conversation. L'agent utilisera tous les messages
            pour construire le contexte, mais seul le dernier message `user`
            déclenchera une nouvelle génération.
          items:
            $ref: '#/components/schemas/Message'
          minItems: 1
          maxItems: 100
        
        conversation_id:
          type: string
          description: |
            Identifiant unique de la conversation pour persistance en mémoire.
            Si omis, un ID sera généré automatiquement.
            **Format recommandé**: `conv-{timestamp}` ou UUID
          pattern: '^[a-zA-Z0-9\-_]+$'
          example: "conv-1698438000"
        
        task_id:
          type: string
          description: |
            Identifiant optionnel de la tâche pour tracking granulaire.
            Utilisé pour lier les Decision Records et la provenance.
          pattern: '^[a-zA-Z0-9\-_]+$'
          example: "task-analysis-001"
        
        model:
          type: string
          description: |
            Nom du modèle à utiliser (compatibilité OpenAI).
            **Note**: Actuellement ignoré, le modèle est configuré via `config/agent.yaml`
          example: "llama-3"
        
        temperature:
          type: number
          format: float
          description: |
            Température de génération (0.0 = déterministe, 2.0 = très créatif).
            **Override**: Remplace temporairement `config/agent.yaml`
          minimum: 0.0
          maximum: 2.0
          example: 0.2
        
        max_tokens:
          type: integer
          description: |
            Nombre maximum de tokens à générer.
            **Override**: Remplace temporairement `config/agent.yaml`
          minimum: 1
          maximum: 10000
          example: 800
      
      example:
        messages:
          - role: user
            content: "Écris un algorithme de tri rapide en Python"
        conversation_id: "conv-12345"
        task_id: "task-quicksort"
        temperature: 0.2
    
    ChatResponse:
      type: object
      required:
        - id
        - object
        - created
        - model
        - choices
        - usage
      properties:
        id:
          type: string
          description: Identifiant unique de la completion
          example: "chatcmpl-1698438000"
        
        object:
          type: string
          enum: [chat.completion]
          description: Type d'objet retourné (compatibilité OpenAI)
          example: "chat.completion"
        
        created:
          type: integer
          format: int64
          description: Timestamp Unix de la création
          example: 1698438000
        
        model:
          type: string
          description: Nom du modèle utilisé
          example: "llama-3"
        
        choices:
          type: array
          description: Liste des choix générés (toujours 1 élément dans FilAgent)
          items:
            type: object
            required:
              - index
              - message
              - finish_reason
            properties:
              index:
                type: integer
                description: Index du choix (toujours 0)
                example: 0
              
              message:
                type: object
                required:
                  - role
                  - content
                properties:
                  role:
                    type: string
                    enum: [assistant]
                    example: assistant
                  content:
                    type: string
                    description: Réponse générée par l'agent
                    example: "Voici un algorithme de tri rapide en Python..."
              
              finish_reason:
                type: string
                enum: [stop, length, tool_calls, error]
                description: |
                  Raison de fin de génération:
                  - `stop`: Génération terminée naturellement
                  - `length`: Limite max_tokens atteinte
                  - `tool_calls`: Arrêt pour exécution d'outils
                  - `error`: Erreur pendant la génération
                example: stop
              
              tools_used:
                type: array
                description: Liste des outils exécutés pendant la génération
                items:
                  type: string
                  enum: [python_sandbox, file_read, math_calculator]
                example: [python_sandbox]
        
        usage:
          type: object
          required:
            - prompt_tokens
            - completion_tokens
            - total_tokens
          properties:
            prompt_tokens:
              type: integer
              description: Nombre de tokens dans le prompt
              example: 30
            completion_tokens:
              type: integer
              description: Nombre de tokens générés
              example: 150
            total_tokens:
              type: integer
              description: Total des tokens utilisés
              example: 180
    
    ConversationHistory:
      type: object
      required:
        - conversation_id
        - messages
      properties:
        conversation_id:
          type: string
          description: Identifiant de la conversation
          example: "conv-12345"
        
        messages:
          type: array
          description: Liste chronologique des messages
          items:
            type: object
            required:
              - role
              - content
              - timestamp
            properties:
              role:
                type: string
                enum: [user, assistant]
                example: user
              
              content:
                type: string
                description: Contenu du message
                example: "Bonjour"
              
              task_id:
                type: string
                description: Task ID associé (si spécifié)
                example: "task-001"
              
              timestamp:
                type: string
                format: date-time
                description: Horodatage ISO8601
                example: "2025-10-27T21:39:00.000Z"
              
              message_type:
                type: string
                enum: [text]
                description: Type de message
                default: text
              
              metadata:
                type: object
                description: Métadonnées additionnelles
                additionalProperties: true
                example:
                  tokens_used: 25
                  tools_used: ["python_sandbox"]
    
    # ==================== Health Schemas ====================
    
    BasicHealthResponse:
      type: object
      required:
        - service
        - version
        - status
      properties:
        service:
          type: string
          description: Nom du service
          example: FilAgent
        version:
          type: string
          description: Version du service
          pattern: '^\d+\.\d+\.\d+$'
          example: "0.1.0"
        status:
          type: string
          enum: [running]
          description: Statut du service
          example: running
    
    DetailedHealthResponse:
      type: object
      required:
        - status
        - timestamp
        - components
      properties:
        status:
          type: string
          enum: [healthy, degraded]
          description: |
            Statut global du service:
            - `healthy`: Tous les composants opérationnels
            - `degraded`: Au moins un composant défaillant
          example: healthy
        
        timestamp:
          type: string
          format: date-time
          description: Timestamp ISO8601 du health check
          example: "2025-10-27T21:39:00.000Z"
        
        components:
          type: object
          required:
            - model
            - database
            - logging
          description: Statut détaillé de chaque composant critique
          properties:
            model:
              type: boolean
              description: |
                Modèle LLM chargé et prêt à générer.
                **False** si: modèle non chargé, erreur d'initialisation
              example: true
            
            database:
              type: boolean
              description: |
                Connexion SQLite opérationnelle pour mémoire épisodique.
                **False** si: fichier DB corrompu, permissions insuffisantes
              example: true
            
            logging:
              type: boolean
              description: |
                Système de logging (EventLogger + WormLogger) fonctionnel.
                **False** si: directories de logs inaccessibles
              example: true
    
    # ==================== Error Schemas ====================
    
    ErrorResponse:
      type: object
      required:
        - detail
      properties:
        detail:
          type: string
          description: Message d'erreur détaillé
          example: "Agent error: Generation timeout after 60s"
        
        error_code:
          type: string
          description: Code d'erreur machine-readable (optionnel)
          example: "GENERATION_TIMEOUT"
        
        trace_id:
          type: string
          description: Trace ID pour debugging (si disponible)
          example: "9b1d1f2a3c4d5e6f"
    
    # ==================== Compliance Components (Documentés) ====================
    
    DecisionRecord:
      type: object
      description: |
        **Decision Record (DR)** - Enregistrement signé d'une décision automatisée.
        
        Conforme à l'article 6.1 de la Loi 25 (Québec) et article 22 du RGPD.
        Chaque DR est signé avec EdDSA (Ed25519) pour garantir l'authenticité.
        
        **Génération automatique** pour:
        - Utilisation d'outils (write_file, delete_file, execute_code)
        - Actions significatives détectées dans la réponse
        - Décisions ayant un impact utilisateur
      required:
        - dr_id
        - ts
        - actor
        - task_id
        - decision
        - signature
      properties:
        dr_id:
          type: string
          description: Identifiant unique du Decision Record
          pattern: '^DR-\d{8}-[a-f0-9]{6}$'
          example: "DR-20251027-abc123"
        
        ts:
          type: string
          format: date-time
          description: Timestamp ISO8601 de la décision
          example: "2025-10-27T21:39:00.000Z"
        
        actor:
          type: string
          description: Acteur ayant pris la décision
          example: "agent.core"
        
        task_id:
          type: string
          description: ID de la tâche associée
          example: "task-001"
        
        policy_version:
          type: string
          description: Version des politiques appliquées
          example: "policies@0.1.0"
        
        model_fingerprint:
          type: string
          description: Hash du modèle utilisé pour reproductibilité
          example: "sha256:abc123..."
        
        prompt_hash:
          type: string
          description: Hash SHA256 du prompt
          example: "sha256:def456..."
        
        decision:
          type: string
          description: Décision prise par l'agent
          example: "execute_python_code"
        
        tools_used:
          type: array
          items:
            type: string
          description: Outils exécutés
          example: ["python_sandbox"]
        
        reasoning_markers:
          type: array
          items:
            type: string
          description: Marqueurs de raisonnement
          example: ["plan:3-steps", "constraints:jsonschema:v2"]
        
        alternatives_considered:
          type: array
          items:
            type: string
          description: Alternatives envisagées
          example: ["do_nothing", "ask_clarification"]
        
        constraints:
          type: object
          additionalProperties: true
          description: Contraintes appliquées
          example:
            timeout_s: 30
            max_tokens: 800
        
        expected_risk:
          type: array
          items:
            type: string
          description: Risques anticipés
          example: ["path_traversal:low", "pii_leak:low"]
        
        signature:
          type: string
          description: Signature EdDSA (Ed25519) du DR
          pattern: '^ed25519:[a-f0-9]+$'
          example: "ed25519:a1b2c3d4e5f6..."
    
    ProvenanceGraph:
      type: object
      description: |
        **Graphe de Provenance W3C PROV-JSON**
        
        Métadonnées de traçabilité complète selon le standard W3C PROV.
        Chaque génération produit un graphe liant:
        - **Entities**: Artefacts (prompts, réponses, fichiers)
        - **Activities**: Processus (génération, exécution d'outils)
        - **Agents**: Acteurs (agent, outils, utilisateur)
        - **Relations**: Liens de dérivation et attribution
        
        **Storage**: `logs/traces/otlp/prov-{task_id}.json`
      required:
        - entity
        - activity
        - agent
      properties:
        entity:
          type: object
          description: Entités (artefacts générés)
          additionalProperties:
            type: object
            properties:
              prov:label:
                type: string
              hash:
                type: string
          example:
            "response:task-001":
              "prov:label": "Response JSON"
              hash: "sha256:abc123..."
        
        activity:
          type: object
          description: Activités (processus)
          additionalProperties:
            type: object
            properties:
              prov:type:
                type: string
              prov:startTime:
                type: string
                format: date-time
              prov:endTime:
                type: string
                format: date-time
          example:
            "gen:task-001":
              "prov:type": "generation"
              "prov:startTime": "2025-10-27T21:39:00.000Z"
              "prov:endTime": "2025-10-27T21:39:05.000Z"
        
        agent:
          type: object
          description: Agents (acteurs)
          additionalProperties:
            type: object
            properties:
              prov:type:
                type: string
              version:
                type: string
          example:
            "agent:llmagenta":
              "prov:type": "softwareAgent"
              version: "0.1.0"
        
        wasGeneratedBy:
          type: array
          description: Relations de génération
          items:
            type: object
            properties:
              prov:entity:
                type: string
              prov:activity:
                type: string
        
        wasAssociatedWith:
          type: array
          description: Relations d'association
          items:
            type: object
            properties:
              prov:activity:
                type: string
              prov:agent:
                type: string
    
    EventLog:
      type: object
      description: |
        **Event Log JSONL** - Événement structuré compatible OpenTelemetry
        
        Chaque ligne dans `logs/events/events-YYYY-MM-DD.jsonl` est un événement.
        Format compatible avec les outils d'observabilité standard (Jaeger, Zipkin).
        
        **Append-only**: Les logs sont écrits via WormLogger pour garantir l'immuabilité.
      required:
        - ts
        - trace_id
        - span_id
        - level
        - actor
        - event
      properties:
        ts:
          type: string
          format: date-time
          description: Timestamp ISO8601
          example: "2025-10-27T21:39:00.431Z"
        
        trace_id:
          type: string
          description: Trace ID pour corrélation distribuée
          pattern: '^[a-f0-9]{16}$'
          example: "9b1d1f2a3c4d5e6f"
        
        span_id:
          type: string
          description: Span ID pour hiérarchie de traces
          pattern: '^[a-f0-9]{8}$'
          example: "e21a3c5d"
        
        level:
          type: string
          enum: [DEBUG, INFO, WARNING, ERROR]
          description: Niveau de log
          example: INFO
        
        actor:
          type: string
          description: Acteur émetteur de l'événement
          example: "agent.core"
        
        event:
          type: string
          description: Type d'événement
          enum:
            - conversation.start
            - conversation.end
            - tool.call
            - generation.complete
            - dr.created
            - pii.detected
          example: "tool.call"
        
        conversation_id:
          type: string
          description: ID de la conversation
          example: "conv-12345"
        
        task_id:
          type: string
          description: ID de la tâche
          example: "task-001"
        
        metadata:
          type: object
          description: Métadonnées additionnelles
          additionalProperties: true
          example:
            tool_name: "python_sandbox"
            success: true

  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        **JWT Token** pour authentification (non implémenté dans MVP).
        
        À implémenter pour production avec:
        - Rotation des secrets
        - Scopes RBAC dans le payload
        - Expiration courte (15min) + refresh token

security:
  - {}  # Pas d'authentification dans MVP (localhost uniquement)
  # Pour production, décommenter:
  # - BearerAuth: []
